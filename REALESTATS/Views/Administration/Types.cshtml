@model IEnumerable<CORE.DtoTypeRealStat>

@{
    ViewBag.Title = "Types";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>أنواع المنشورات</h2>

<p>
    @Html.ActionLink("إضافة نوع ", "AddType",new { }, new { @class = "btn-rnd" })
</p>
<table class="table  table-striped table-responsive">
    <thead>
        <tr>
            <th>
                @Resource.ID
            </th>
            <th>
                @Resource.libelleHeaderType
            </th>

            <th></th>
        </tr>
    </thead>
    
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.id_type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.type_ar)
                </td>

                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }, new { @class = "btn-rnd-success" })

                </td>
            </tr>
        }

    </tbody>
</table>
